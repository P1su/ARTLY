import React from 'react';
import SectionTitle from '../../../../components/SectionTitle/SectionTitle';

export default function SectionCalendar({ viewed }) {
  return (
    <section>
      <SectionTitle title='전시 캘린더' />
    </section>
  );
}
